import { AstNode } from 'ast-types';

class PrettierService {
    public async loadOptionsForFile(fileName: string) {}

    public formatAstNode(node: AstNode) {}
}

export default new PrettierService();
